export enum Response {
    invalid_min = "Invalid minimum length must be above or equal to ",
    invalid_max = "Invalid maximum length must be below or equal to ",
    empty = "Empty field.",
    success = "",
}